<section class="section bg-white" id="contact" style="padding: 64px 0;">
  <div class="container-page">
    <div class="rounded-[28px] bg-[#0E1114] text-white px-6 md:px-10 py-16 md:py-20 text-center">
      <h2 class="text-2xl sm:text-3xl md:text-4xl font-semibold tracking-tight">Ready to Get More Customers?</h2>
      <p class="mt-4 text-lg text-white/85">Fill in our simple form to get started today.</p>
      
      <div class="mt-8 flex flex-col sm:flex-row gap-4 justify-center">
        <a href="/intake" 
           class="button-primary-on-dark relative"
           x-data="{ loading: false }"
           @click="loading = true">
          <span x-show="!loading">Start Your Project</span>
          <span x-show="loading" class="flex items-center gap-2">
            <svg class="animate-spin h-4 w-4" fill="none" viewBox="0 0 24 24">
              <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
              <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
            </svg>
            Loading...
          </span>
        </a>
        <a href="https://wa.me/27614631005" class="button-secondary-on-dark" target="_blank" rel="noopener">WhatsApp Us</a>
      </div>
      
      <div class="mt-6 text-sm text-white/80">
        <span class="inline-flex items-center gap-2">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
Website ready for review within 48 hours
        </span>
      </div>
    </div>
  </div>
</section>

