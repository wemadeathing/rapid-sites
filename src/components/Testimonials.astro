<section class="section bg-white" id="testimonials">
  <div class="container-page">
    <div class="text-center max-w-3xl mx-auto">
      <h2 class="text-4xl font-semibold text-neutral-900">What our clients say</h2>
      <p class="mt-4 text-lg text-neutral-600">Real results from real businesses across South Africa</p>
    </div>
    
    <div class="mt-12" x-data="{
      currentSlide: 0,
      init() {
        console.log('Testimonial carousel initialized');
        setInterval(() => {
          this.currentSlide = (this.currentSlide + 1) % 3;
          console.log('Current slide:', this.currentSlide);
        }, 8000);
      }
    }">
      <!-- Carousel Container -->
      <div class="relative overflow-hidden">
        <div 
          class="flex transition-transform duration-800 ease-in-out"
          :style="`transform: translateX(-${currentSlide * 100}%)`"
        >
          <!-- Testimonial 1 -->
          <div class="w-full flex-shrink-0">
            <div class="max-w-5xl mx-auto bg-white rounded-2xl p-7 md:p-10 border-2 border-neutral-900">
              <div class="flex items-start gap-5">
                <img src="https://i.pravatar.cc/120?img=12" alt="Thabo M." class="h-16 w-16 rounded-full" loading="lazy" />
                <div>
                  <p class="text-neutral-800 text-lg leading-relaxed">"It doesn't get much easier than this. From brief to live in two days, and enquiries doubled. Clean, fast, and Google-ready from day one."</p>
                  <div class="mt-3">
                    <div class="font-semibold text-neutral-900">Thabo M.</div>
                    <div class="text-sm text-neutral-600">Construction • Johannesburg</div>
                  </div>
                </div>
              </div>
            </div>
          </div>
          
          <!-- Testimonial 2 -->
          <div class="w-full flex-shrink-0">
            <div class="max-w-5xl mx-auto bg-white rounded-2xl p-7 md:p-10 border-2 border-neutral-900">
              <div class="flex items-start gap-5">
                <img src="https://i.pravatar.cc/120?img=3" alt="Sarah K." class="h-16 w-16 rounded-full" loading="lazy" />
                <div>
                  <p class="text-neutral-800 text-lg leading-relaxed">"Finally, a website that actually works for my business. The mobile experience is perfect and I'm getting calls from customers who found me on Google."</p>
                  <div class="mt-3">
                    <div class="font-semibold text-neutral-900">Sarah K.</div>
                    <div class="text-sm text-neutral-600">Beauty Salon • Cape Town</div>
                  </div>
                </div>
              </div>
            </div>
          </div>
          
          <!-- Testimonial 3 -->
          <div class="w-full flex-shrink-0">
            <div class="max-w-5xl mx-auto bg-white rounded-2xl p-7 md:p-10 border-2 border-neutral-900">
              <div class="flex items-start gap-5">
                <img src="https://i.pravatar.cc/120?img=7" alt="David L." class="h-16 w-16 rounded-full" loading="lazy" />
                <div>
                  <p class="text-neutral-800 text-lg leading-relaxed">"Professional service from start to finish. They understood my business needs and delivered exactly what I wanted. My online presence has never been stronger."</p>
                  <div class="mt-3">
                    <div class="font-semibold text-neutral-900">David L.</div>
                    <div class="text-sm text-neutral-600">Consulting • Durban</div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
      
      <!-- Navigation Dots -->
      <div class="flex justify-center mt-8 gap-2">
        <button 
          @click="currentSlide = 0"
          :class="currentSlide === 0 ? 'bg-neutral-900' : 'bg-neutral-300'"
          class="w-3 h-3 rounded-full transition-colors"
        ></button>
        <button 
          @click="currentSlide = 1"
          :class="currentSlide === 1 ? 'bg-neutral-900' : 'bg-neutral-300'"
          class="w-3 h-3 rounded-full transition-colors"
        ></button>
        <button 
          @click="currentSlide = 2"
          :class="currentSlide === 2 ? 'bg-neutral-900' : 'bg-neutral-300'"
          class="w-3 h-3 rounded-full transition-colors"
        ></button>
      </div>
    </div>
  </div>

</section>

