---
import heroSvgUrl from '../assets/hero.svg?url';
---
<section id="about" class="bg-white" style="padding-bottom: 64px;">
  <div class="container-page">
    <div class="relative rounded-[28px] overflow-hidden px-6 md:px-8 py-10 md:py-12">
      <!-- Flat teal background -->
      <div class="absolute inset-0 surf-hero-bg"></div>

      <div class="relative grid grid-cols-1 md:grid-cols-2 gap-10 items-center">
        <!-- Left column -->
        <div class="max-w-3xl">
          <div class="mb-4">
            <div class="inline-flex items-center px-3 py-1 rounded-full bg-white/30">
              <span class="text-sm text-black font-medium">Launch Special Price R5,500</span>
            </div>
          </div>
          
          <h1 class="text-4xl sm:text-5xl md:text-5xl font-semibold tracking-tight text-black">Get Your SA Business Found Online Fast</h1>
          <p class="mt-4 lead text-black">Professional websites that get South African businesses found online and generate leads. Built in 48 hours with clean, SEO-optimized code, mobile-first design, and zero endless revision cycles.</p>
          <div class="mt-8 flex flex-wrap items-center gap-3">
            <a href="#contact" class="inline-flex items-center justify-center rounded-[9999px] px-5 py-3 text-sm font-medium text-white bg-black hover:bg-neutral-800 focus:outline-none focus:ring-2 focus:ring-neutral-900 focus:ring-offset-2 transition-colors">Get Your Website</a>
            <a href="#how-it-works" class="inline-flex items-center justify-center rounded-[9999px] px-5 py-3 text-sm font-medium text-black border-2 border-black bg-transparent hover:bg-black/10 focus:outline-none focus:ring-2 focus:ring-neutral-900 focus:ring-offset-2 transition-colors">See the Process</a>
          </div>
        </div>

        <!-- Right image -->
        <div class="relative md:justify-self-end self-center w-full md:max-w-[400px] lg:max-w-[440px]">
          <div class="rounded-2xl overflow-hidden aspect-square min-h-[220px] md:min-h-[320px] lg:min-h-[360px] w-full">
            <img src={heroSvgUrl} alt="People working together in a modern business environment" class="w-full h-full object-cover" loading="eager" />
          </div>
        </div>
      </div>
    </div>
  </div>

  <style>
    .surf-hero-bg {
      background: var(--color-brand-700); /* flat teal #1EBFBF */
    }
  </style>
</section>

